#!/usr/bin/env python3
"""petal-lab 命令行入口。

用法见 `petal-lab --help`。此脚本不依赖任何第三方包，
只需系统 Python 3.9+。
"""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from petal_lab.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
